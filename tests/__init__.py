"""Test package for ``youtube-helper``.

Marks ``tests/`` as an importable package so pytest can resolve shared
fixtures and relative imports if the suite grows. Intentionally empty
otherwise — the test modules themselves carry the logic.
"""
